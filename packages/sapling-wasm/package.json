{
  "name": "@airgap/sapling-wasm",
  "author": "Papers.ch <contact@papers.ch>",
  "version": "0.0.9",
  "homepage": "https://www.airgap.it",
  "repository": {
    "type": "git",
    "url": "https://github.com/airgap-it/airgap-sapling"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "/dist"
  ],
  "main": "dist/index.node.js",
  "browser": "dist/index.browser.js",
  "types": "dist/index.d.ts",
  "sourceMap": true,
  "scripts": {
    "build": "rm -rf dist && rm -rf pkg && npx webpack --mode=development && npm link ./dist",
    "build:prod": "rm -rf dist && rm -rf pkg && npx webpack --mode=production"
  }
}
